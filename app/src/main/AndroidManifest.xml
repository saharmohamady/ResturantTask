<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quand.resturanttask">
    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver android:name=".utilities.CleanAlarmReceiver">
            <intent-filter>
                <action android:name="packagename.ACTION" />
                <action android:name="packagename.ACTION2" />
            </intent-filter>
        </receiver>


        <activity
            android:label="@string/Customers"
            android:name=".activities.CustomersActivity"
            android:windowSoftInputMode="stateHidden">

        </activity>
        <activity android:name=".activities.TablesActivity"
            android:label="@string/Tables"
            android:parentActivityName=".activities.CustomersActivity"
            ></activity>
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>